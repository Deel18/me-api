build:
  environment:
    node: v12.10.0
    variables:
      JWT_SECRET: 'LONGESTSECRETEVER'
filter:
    excluded_paths:
        -"mode_modules/"
        -"coverage/"
  tests:
    override:
      - command: npm test
        coverage:
          file: coverage/clover.xml
          format: clover
